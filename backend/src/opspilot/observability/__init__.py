"""OpenTelemetry instrumentation (Phase 11).

Spans for request handling, retrieval, reranking, LLM calls, and tool calls;
latency and token counters; error recording. Secrets and document contents
are never attached to spans.
"""
